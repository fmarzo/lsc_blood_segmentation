ViT
CLIP
AlexNet
Resnet
Forward pass?

DOMANDA 1 (Biliardi):
Mechanism of 2D convolutional layer (non dimentichi un operazione? -> il round) 
	- Which are the input and kernel shapes (in terms of tensor)?
	- Impariamo dei bias nei layer convolutivi? (1 per ogni kernel (oC))
	
Non sapeva le basi, grandissima difficoltà. Baraldi un po' innervorsito da questa cosa, gli hai dato 23 per PURA grazia divina.


DOMANDA 2 (Davoli):
Definition of Attention operator
	- mandatory numbers of query and keys?
		. recall of cross attention in transformer paper
	- dilated convolution? (risposta: enlarge receiptive field without increasing number of params)
		. grouping?
		
Lui sicuro, risposte puntuali. 30.
		
DOMANDA 3 ():
	- transpose convolution (semantic segmentation)
		. older approach which brought to transpose (bed of nails, maxpooling) + vantaggi della transpose convolution
	- DeT:
		. how do you train queries? 
	
	

DOMANDE PRE MEDIEVIL

Domande CV

22 / 12 / 2025

- Draw a gaussian kernel 
- input, kernel, bias and output shape of a 2D convolutional layer in a CNN (remember the batch size)
- CLIP
- minimum value of the cosine similarity (-1)
- name of the loss in CLIP (contrastive loss)
    - how it is implemented 
- attention 
- ResNet and its innovation (residual connection)
    - draw the architecture
    - how can I downsample (stride = 2)
    - draw a version of it that does downsampling 
    - usual kernel shape in a ResNet (3x3)


- architecture of Inception Net
	
 

- 3D convolutional layer with shapes (remember pseudo 3D network)
- can you do object detection with attention? (DETR)
    - how do you implement the object queries? (learnable through fully connected layers)
    - do you have constraints on the number of object queries? (correlated to the maximum number of detection that can be done in an image)
- difference between self attention and cross attention 
- how does a bilateral filter works 
- definition of an edge (point in which there is an high variation of luminosity)
    - how do you compute it practically? (masked, sobel (the formula gives the magnitude of the gradient))
- how do you do object detection with a CNN? (RCNN)
    - how does the Roi pooling work?
- Roi align and difference with respect to the Roi pooling 
- optical flow 
    - example in which the optical flow doesn’t match the motion field, example in which they are not equal (barber poll)



16 / 1 / 2026


- draw a convolutional kernel 
- gaussian kernel 
	0 1 0
	1 2 1
	0 1 0 
- input, output, kernel shape of the convolution 

- 3D convolutional kernel input, output, kernel shape 
- shape of the kernel of the pseudo conv 
- self-attention (with shapes)

- how does the optical flow works?
- difference between sparse and dense optical flow methods (prefer the dense optical flow to put it as input of a CNN or whatever)
- cross-attention
- final length of the output of the cross-attention layer (the output has the same shape as the input)
- ViT
- compare a ViT with a CNN (advantages and disadvantages) 
    - CNN: local - Trasnformer: global 
    - CNN: downsample - Transformer: isotropic 
    - CNN: less data - Transformer: more data 
    - CNN: high inductive bias - Transformer: low 
- object detection with Transformers (DETR)

- which metrics or measures you employ to compute the similarity in an image retrieval system 
- formula for the norm 
- what’s an image histogram
- residual block
- SIFT descriptor

- inception block in GoogleNet
- edge detection (first derivative, sobel kernel, kenny)
- single difference and double difference in motion 
- example in which the double difference perform better than the single difference 
- what’s the optical flow 
- RoI pooling layers

- how does CLIP work
- how to encode images? = what to use if we want a good descriptor for images? = SOTA encoders for images? (CNN, ViT)
- attention 

- CLIP
    - image encoder (ViT, modified ResNet)
    - text encoder (transformer encoder)
- norm L2
	x = [1, 0, 2]
	L2-normed vector = [1/√5, 0, 2/√5] 
- cosine similarity 
- training procedure of CLIP 
- loss function of CLIP (there’s one for the images and one for the text, choose one of them, and show)
    - how do you compute the cross-entropy loss? (InfoNCE) 
- RoI Align 
- RoI Align and Roi Pooling convert things that have a variable shape into things that have a constant shape, explain this


22/06/2026

Progetti

Progetto 1: Face Recognition (Vit vs Cnn based models)
- analisi del dataset (dimension, class imbalance...)
- breve descrizione dei modelli 
- analisi delle performance

- i ragazzi hanno usato due approcci diversi per due modelli che hanno trainato. es per il primo modello finetuning per ogni livello mentre nel secondo hanno i primi livelli frozen e hanno finetunato sui restanti. Non hanno saputo spiegare perchè hanno fatto questo. Hanno comparato due modelli diversi con due protocolli diversi. Potevano volendo applicare lo stesso approccio per entrambi i modelli. 

- per i modelli ha diviso il dataset in 3 parti (train validation training), il problema è che ha fatto questo per tutto i modelli. Ma non ha la certezza che gli item estratti per il training siano gli stessi usati per tutti i modelli. Senza questa certezza potrebbero aver comparo performance di modelli trainati su dataset di train con item diversi.

- come si scelgono il numero di epoch?guardando la validation loss e confrontando i risultati con numero di epoch diverse. Guardando la loss decido se aumentare o no. Troppe causano overfitting. (epoch: ciclo completo dell'algoritmo di training, quante volte aggiorno i paramentri completamente) 

Progetto2: image generation (LoRa vs Textual Inversion)
- ha mostrato un esempio (es delle immagini di gatti generate dai due modelli) ma non ha mostrato una valutazione molto tecnica.

Progetto3: video analysis (detecting anda tracking) (straniero)
- hanno fatto anche un software che usa il loro modello e lo hanno fatto vedere a lezione: tracking di giocatori. Anche loro non hanno mostrato una valutazione tecnica ma solo qualitativa. 
- hanno usato il modello solo su un dataset su giocatori di basket e non hanno comparato con altri.
- l'approccio deve essere scientifico, non solo fare le cose ma anche valutare rispetto anche approcci già esistenti...altri dataset...

Progetto4:LLaVa vs Qwen (straniero)
- queste non sapevano quello che avevano fatto (spagnole)

Progetto5: Face Recognition (straniero)
- non hanno specificato lo splitting del dataset e i modelli devono essere comparati sugli stessi dati
- hanno trainato solo una resnet troppo poco. Dicono che Livia abbia dato l'ok. (è sufficiente ma non è comparabile con i progetti dello scorso anno).



Domande

- RESNET
    - what issues can solve a resnet? vanishing gradient
    - if i want downsample in a resnet, how? using a stride > 1. 
    - if i do downsample how i manage the sum with the residual connection? adding a conv in the residual connection with the same stride

- how does compute grouping in a conv layer?

- 3d conv and differenze with 2d conv (non sono state chieste le formule degli shape)

- pseudo 3d conv residual network: from 3x3x3 kernel to a 1x3x3 + 3x1x1

- what a tracking does? (straniero)

- difference between detection by tracking and tracking by detection (straniero)

- difference with object detector (it gives coordinates of the bounding box). The tracker follow a detection over time. (straniero)   

- what is SAM (straniero)

- Homography transformation? what geometrical trasformation am i applyng? (a perspective trasformation) (straniero)

- transpose convolution (straniero)

- Faster R-CNN and comparing with R-CNN, Fast R-CNN... (straniero)

- Attention (straniero)

- ViT: (straniero)
    - difference with CNN

- bilateral filters: it is a non linear filters...(straniero)
    - what it does
 
- transpose convolution (straniero)

- Attention mechanism: formula...(straniero)

- 3D conv (straniero)

- what the effect of the stride in the normal CNN vs the effect in the transpose conv (straniero) (fatta anche a un ragazzo dello scorso anno)

- how i train clip and what is clip loss? (fatta a un ragazzo dello scorso anno)
    - the final matrix that contains the cosine simililarity (computed between the image and text embeddings, we have a vector for each image and text and not a number) 
    - how do i compute the cosine sim?formula

- image processing: adaptive thresholding (fatta a un ragazzo dello scorso anno)

- components of ViT and attention formula (softmax(Q*KT)/sqrt(dk))
    - Q = X*wq, X is a vector or a matrix?(a matrix)
    - what is the softmax in the formula 
    - what links the output of the encoder as a input for the decoder?the cross attention